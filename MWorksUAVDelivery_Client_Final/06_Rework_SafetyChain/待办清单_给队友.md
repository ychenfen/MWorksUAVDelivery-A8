# 挑战赛剩余待办（截止 8 月 1 日 09:00）

文档、代码、数据、视频四条线已经改完并对齐。剩下的分两类：**必须在 MWorks 上跑的**，和**只有你们能提供的材料**。

---

## 一、MWorks 上要跑的（这是关键路径）

先把两个新模型加载进来：

```
06_Rework_SafetyChain/UAVSafetyChain.mo
06_Rework_SafetyChain/UAVFormation.mo
```

它们依赖原有的 `QuadrotorModel.mo`，请先加载原模型再加载这两个。

### 第 0 步：等价性回归（不过就别往下做）

**跑 `UAVSafetyChain.Task.ClimbEquivalenceCheck`**（StopTime=50，Interval=0.01，Dassl）

这个模型把安全链全关掉、倾角限幅改回 9.5°，理论上应该和原来的 `QuadrotorTask.ClimbEnhanced` 一模一样。

- **判据：path_rmse = 0.23289**（与 metrics_summary.json 里 climb_enhanced 一致）
- 对得上 → 说明我重写的控制器与原控制器等价，后面的结论才可信
- 对不上 → **立刻停下告诉我**，把 CheckModel 报错或数值差异发我，我来修。这一步不过，后面全部白跑

### 第 1 步：ERG 层（零重连线，风险最低）

跑这三个：

| 模型 | StopTime |
|---|---|
| `UAVSafetyChain.Task.ClimbSafeERG` | 50 |
| `UAVSafetyChain.Task.SpiralSafeERG` | 50 |
| `UAVSafetyChain.Task.EightSafeERG` | 120 |

**要记录的指标**（每个场景都要）：

- 路径 RMSE、平均误差、最大误差
- roll / pitch 的最大绝对值（度）
- **15° 超限的累计时长**（不是点数，点数会随输出间隔变）
- 阶跃超调（爬升场景）

**判据**（跑之前就定死的，不要事后调）：

1. 三个场景的 15° 超限累计时长 = 0
2. 各场景 path_rmse 相对原增强模型劣化不超过 3%
   - 原值：爬升 0.23289 / 螺旋 0.48227 / 8字 0.15290

### 第 2 步：完整安全链

| 模型 | StopTime |
|---|---|
| `UAVSafetyChain.Task.ClimbSafeFull` | 50 |
| `UAVSafetyChain.Task.SpiralSafeFull` | 50 |
| `UAVSafetyChain.Task.EightSafeFull` | 120 |

除上述指标外，这一层还会输出 `violationTime`（超限累计时长）和 CBF 修正量，一并记录。

### 第 3 步：消融对照（用来说清各模块各自的贡献）

| 模型 | 作用 |
|---|---|
| `UAVSafetyChain.Task.ClimbERGOnly` | 只开限速 |
| `UAVSafetyChain.Task.ClimbCBFOnly` | 只开障碍函数 |
| `UAVSafetyChain.Task.ClimbBaselineERG` | 基线 PID + 限速，分离"限速的贡献"与"滑模的贡献" |

### 第 4 步：编队（能跑就跑，跑不动跳过）

`UAVFormation.Examples.ThreeUAVFormation`（StopTime=120）
另有 `FormationDisturbanceTest`（扰动恢复）和 `FormationCollisionTest`（避撞对抗）。

注意：这个模型里的单机是降阶二阶模型，不是完整多体机体。要出可写进报告的编队精度绝对值，
需要按 `UAVFormation.mo` 顶部文档说的方法换成完整机体——那个改动量不小，时间不够就先不做，
报告里现在的措辞已经把这个边界写清楚了。

### 第 5 步：重新导出数据（很重要，别漏）

**用同一次运行，一次性重新生成 CSV 和全部指标。**

现在交付包里有个硬伤：三个 enhanced 场景的 CSV 是 6 月 2 日重跑的，但 `metrics_summary.json`
还是 5 月 30 日的旧值，两边 path_rmse 差 1.63%~2.49%，姿态超限点数也对不上（JSON 记 2 个，
用 CSV 复算是 3 个）。评委拿 CSV 一复算就会发现报告的数字对不上。

所以重跑时请：

1. 六个原模型（baseline / enhanced × 三场景）也一并重跑
2. 用 `mworks_export_results.py` 一次性导出 CSV 和指标，**不要只更新其中一边**
3. 导完自己抽查一下：随便挑一个场景，用 CSV 算一遍 RMSE，看是否和 JSON 里一致

---

## 二、只有你们能提供的材料

| 项 | 说明 |
|---|---|
| **报名表** | 我这边没有这个文件，按官网要求填 |
| **团队编号** | 邮件标题要用，我不知道你们的编号 |
| **报告目录刷新** | 用 Word 打开 `仿真报告_返工稿.docx`，选中目录按 F9 更新。目录是手工做的不会自动更新，新增了 6.5 节、页码也变了 |

---

## 三、提交方式（官网明确写了的，容易踩坑）

- 上传到**个人百度网盘**，要**分享文件夹、可在线打开**，官网写明**尽量不要上传压缩包**
- 生成**加密**提取链接，链接和提取密码**直接写在邮件正文**，不要放进文档里当附件传
- 收件：`cnsoftbei@qq.com`
- 邮件标题：`A8+团队编号+团队名称+西安交通大学城市学院`
- 如果你们是「三等奖第一批」想争取去现场，标题前面加 `三等奖第一批+`

**上传时注意**：`06_Rework_SafetyChain/_withdrawn_初赛造假产物/` 这个目录**不要上传**，
它是留档用的（133 MB），里面是已作废的旧代码和数据。

---

## 四、可选（时间够才做）

- **视频 2:43 处的话音跳变**：切点在那里硬切，解说会断一下。重录一小段过渡会更自然，来不及就这样交，内容上是干净的。
- **第 4 章表 1 的数据源**：现在表 1 用的是 `metrics_summary.json`，第 6 章用的是交付 CSV，两边差 1.63%~2.49%。我在 6.3 节如实写明了差异。如果第 5 步的重跑做了，两边就统一了，那时候把表 1 一起更新；如果没时间重跑，就保持现状 + 保留那段说明。

---

## 跑完之后

把每个模型的指标（RMSE、最大姿态角、超限累计时长）整理成一张表发我，我来填进报告 6.5 节和手册的对应位置，并把"待重跑填入"的占位换成真实数字。

如果第 0 步等价性回归就没过，**先别跑后面的**，直接把报错发我。
